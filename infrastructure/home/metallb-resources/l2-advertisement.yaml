---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: home-l2-advertisement
  namespace: metallb-system
spec:
  ipAddressPools:
    - home-pool
  # Removed interfaces restriction to allow MetalLB to use all available interfaces

